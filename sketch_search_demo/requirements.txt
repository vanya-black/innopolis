streamlit>=1.49
numpy>=1.24
pillow>=10.0

# Холст для рисования. ВАЖНО: оригинальный streamlit-drawable-canvas (0.9.3)
# заброшен и падает на свежем Streamlit — используем поддерживаемый форк
# с тем же import-путём.
streamlit-drawable-canvas-fix>=0.9.8

# CLIP-эмбеддинги фотографий и рисунка (sentence-transformers, как в demo №1)
sentence-transformers>=3.0
torch>=2.0

# Сбор галереи (collect.py): parquet с фото + названия категорий из WordNet
huggingface-hub>=0.23
pyarrow>=15.0
nltk>=3.8
